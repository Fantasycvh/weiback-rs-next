[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "weiback-collector"
version = "0.4.0"
description = "WeiBack Next headless JSONL sidecar (protocol v1)"
requires-python = ">=3.11"
dependencies = []

[tool.setuptools]
packages = ["weiback_collector"]
