# v0.5.1 — 增量同步断点修复 + 评论全量接口

## 修复

- **增量断点生效**: 自动/定时同步按 `last_id` 截断，遇到已同步旧帖即停，不再全量翻页到上限
- **评论改走全量接口**: 从 `m.weibo.cn/api/comments/show`（仅最近约 20 条）切换到 `weibo.com/ajax/statuses/buildComments`，按 `max_id` 游标翻页抓取全部评论
- **评论时间保留**: `created_at` 完整解析并入库，不再丢失
- **类型过滤空页继续**: 手动抓取按内容类型过滤时，整页被过滤会继续翻页而非误判结束
- **回复树深度**: 二级回复回复已有评论时正确嵌套（depth 递增），兼容 `reply_comment_id` 字段

## 新功能

- 手动抓取范围（用户/内容类型/页数/评论条数），借鉴 Rust 版 OnlineBackup 设计
- 单帖评论回补（帖子详情页按钮）
- 后台任务跟踪（任务状态接口 + 前端轮询）

## 已验证

- pytest 131 项全部通过
- 真实接口冒烟：buildComments 单帖抓到 40 条评论且全部带时间；增量同步断点即停
- PyInstaller 打包 exe 冒烟通过（Web GUI /backup 正常、`--backfill` 实跑成功）

## 发布说明

未签名候选草稿。正式发布门禁会拒绝未签名工件。
